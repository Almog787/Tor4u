name: Auto Update README with Riva Yehezkel QR

on:
  push:
    branches:
      - main
    paths:
      - 'config.txt'
      - 'index.html'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Extract URL and Update README
        run: |
          # 1. Fetch the target URL from config.txt
          if [ -f config.txt ]; then
            TARGET_URL=$(cat config.txt | xargs)
          else
            TARGET_URL="https://www.riba.co.il/career/"
          fi
          
          # 2. Set the Live Site URL (GitHub Pages)
          LIVE_SITE_URL="https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
          
          # 3. Logo URL (Raw path)
          LOGO_URL="https://github.com/Almog787/Tor4u/blob/23806039964ab720efdbf137719691ed3c08e113/logoo-100.png?raw=true"
          
          # 4. Last update timestamp (Israel Time)
          LAST_UPDATE=$(date -u -d "+2 hours" +'%d/%m/%Y %H:%M')

          # 5. Generate README.md content
          echo '<p align="center">' > README.md
          echo "  <img src=\"$LOGO_URL\" width=\"200\" alt=\"Riva Yehezkel Logo\">" >> README.md
          echo "</p>" >> README.md
          
          echo "# 📲 מערכת QR דינמית - ריבה יחזקאל בקרות" >> README.md
          echo "" >> README.md
          echo "ניהול הפניה דינמית עבור משרות דרושים. כל שינוי בקובץ \`config.txt\` מעדכן אוטומטית את יעד ה-QR ב-README." >> README.md
          echo "" >> README.md
          
          echo "## 🤳 קוד ה-QR לסריקה" >> README.md
          echo '<p align="center">' >> README.md
          echo '  <img src="./job_qr_code.png" width="300" alt="Recruitment QR Code">' >> README.md
          echo '</p>' >> README.md
          echo "" >> README.md
          echo "---" >> README.md
          echo "" >> README.md
          
          echo "## 📊 סטטוס ומידע טכני" >> README.md
          echo "| נתון | פרטים |" >> README.md
          echo "| :--- | :--- |" >> README.md
          echo "| **סטטוס הפניה** | פעיל ✅ |" >> README.md
          echo "| **יעד הפניה נוכחי** | [$TARGET_URL]($TARGET_URL) |" >> README.md
          echo "| **כתובת הדף (Live)** | [$LIVE_SITE_URL]($LIVE_SITE_URL) |" >> README.md
          echo "| **עדכון אחרון** | $LAST_UPDATE |" >> README.md
          echo "" >> README.md
          echo "---" >> README.md
          echo "### 💡 הנחיות למנהל המערכת" >> README.md
          echo "כדי לשנות את היעד אליו ה-QR שולח את המועמדים, יש לערוך את הקובץ [config.txt](./config.txt) במאגר זה." >> README.md

      - name: Commit and Push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "Bot: Updated README with Logo and Redirect Status" || echo "No changes to commit"
          git push
